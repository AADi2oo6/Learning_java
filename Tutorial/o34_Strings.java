public class o34_Strings {
    public static void main(String args[])
    {
        //note string is a class not a datatype: 
        String str = new String("Aditya"); // is the real way of creating an string 
        //strings are immutable but NOTE there are some Strings which are mutable like stringbuffer and stringBuilder
        System.out.println(str);
    }
    
}
